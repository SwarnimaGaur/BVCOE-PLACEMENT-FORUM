<%= form_for @upvotes.build do |f| %>
  <%= f.hidden_field :user_id, value: current_user %>
  <%= f.hidden_field :experience_id, value: e %>

  <%= f.submit 'Upvote' %>
<% end %>

<%= form_tag url_for(:controller => "upvote",:action => "create",:experience_id => "2")do%>    
    <%= hidden_field_tag :user_id, user_id %>
    <%= submit_tag %>
    <!-- shduoiashdpojaspoj -->
<% end %>
